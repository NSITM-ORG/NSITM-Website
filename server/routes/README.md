// Routes structure documentation
// Store all API route definitions here
// Example structure:
//
// import { createUser, getUsers } from '../controllers/userController.js';
// const router = express.Router();
// router.post('/', createUser);
// router.get('/', getUsers);
// export default router;
//
// Routes define the API endpoints and their corresponding controller methods
// Each route file should handle one resource type
// File naming convention: camelCase.js (e.g., userRoutes.js, studentRoutes.js)
// Then import and use in index.js: app.use('/api/users', userRoutes);
